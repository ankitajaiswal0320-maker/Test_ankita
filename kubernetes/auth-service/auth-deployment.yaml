apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: auth
spec:
  replicas: 2
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:

      nodeSelector:
        role: backend

      tolerations:
      - key: "backend"
        operator: "Equal"
        value: "true"
        effect: "NoSchedule"

      containers:
      - name: auth-service
        image: 509521484072.dkr.ecr.eu-north-1.amazonaws.com/auth-service:latest
        ports:
        - containerPort: 5000